export default defineAppConfig({
  ui: {
    // `rubit` is defined as a Tailwind v4 @theme scale in app/assets/css/main.css,
    // seeded on #0B5CD5 - the blue of the app icon at packaging/icons/rubit-mcp-mail.png.
    colors: {
      primary: 'rubit',
      neutral: 'zinc',
    },
  },
})
